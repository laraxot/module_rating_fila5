---
title: "Bozza storica — refactor host->trait (pointer)"
status: superseded-pointer
module: Rating
updated: 2026-09-16
---

# Bozza intermedia — non usare

Storico: proponeva `getRatingsFormState()`/`syncRatingsFromFormState()`/
`validateMinimumPositiveRatings()`/`getRatingsForContext()`. Il ragionamento
"generale vs particolare" era corretto; nomi e scope sono stati rivisti in implementazione.

**Canon:** [5.147-hydrate-sync-ratings-form-data.story.md](5.147-hydrate-sync-ratings-form-data.story.md)
(nomi reali: `hydrateRatingsFormData`/`syncRatingsFormData`; `getRatingsForContext` non
creato, si riusa `getRatingsWhere()` già esistente; `validateMinimumPositiveRatings` non
estratto in questo giro — resta IR-specifico).

Nota numerazione (aggiornata più volte da agenti concorrenti nello stesso giro — verificare
sempre `find docs/bmad/stories -iname "5.14*"` sul disco, non fidarsi di un numero citato a
memoria): il numero libero per questo contenuto è **5.147**, non 5.146 (occupato nel
frattempo da `5.146-select-textarea-section-ui.story.md`, argomento diverso, UI).
